# Pinned scientific stack — a coherent, ABI-consistent set that ships
# prebuilt wheels for every current Python (3.10–3.13). This avoids two
# failure modes at once: (a) Streamlit Cloud's numpy ABI-mismatch segfault
# from unpinned re-resolution, and (b) Railway compiling pandas from source
# (metadata-generation-failed) when no wheel matches its Python version.
numpy==2.1.3
pandas==2.2.3
matplotlib==3.9.2
openpyxl
requests
rapidfuzz
pycountry
python-dotenv
streamlit
rispy
networkx
plotly

# Phase 1 backend (FastAPI + Celery/Redis + Postgres) - merged in here
# because Railway's default Python builder (Nixpacks) only installs from
# a single requirements.txt at the repo root; keeping backend/requirements.txt
# as a separate file (still present, for local-dev reference) would leave
# these uninstalled on Railway and the web/worker services would crash on
# import.
fastapi
uvicorn[standard]
celery
redis
sqlmodel
psycopg2-binary
boto3
python-multipart
