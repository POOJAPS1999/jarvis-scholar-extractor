# Pinned scientific stack — unpinned versions let uv re-resolve on every
# reboot and occasionally pulls a numpy build whose ABI mismatches the
# pandas/matplotlib wheels, segfaulting Streamlit on import. This known-good,
# all-numpy-1.x combo makes the build deterministic.
numpy==1.26.4
pandas==2.2.2
matplotlib==3.8.4
openpyxl
requests
rapidfuzz
pycountry
python-dotenv
streamlit
rispy
networkx
plotly

# Phase 1 backend (FastAPI + Celery/Redis + Postgres) - merged in here
# because Railway's default Python builder (Nixpacks) only installs from
# a single requirements.txt at the repo root; keeping backend/requirements.txt
# as a separate file (still present, for local-dev reference) would leave
# these uninstalled on Railway and the web/worker services would crash on
# import.
fastapi
uvicorn[standard]
celery
redis
sqlmodel
psycopg2-binary
boto3
python-multipart
