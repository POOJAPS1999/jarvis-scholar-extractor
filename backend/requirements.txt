# Phase 1 backend additions - on top of the existing pipeline requirements
# (pandas, openpyxl, requests, rapidfuzz, pycountry, python-dotenv), which
# still apply since this backend calls straight into bibliometric_pipeline.
fastapi
uvicorn[standard]
celery
redis
sqlmodel
psycopg2-binary
boto3
python-multipart
