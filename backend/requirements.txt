# NOTE: these packages are now merged into the root requirements.txt
# (../requirements.txt) too, because Railway's Python builder only installs
# from a single requirements.txt at the repo root - see the comment there.
# This file is kept as a reference for exactly which packages Phase 1 adds
# on top of the base pipeline requirements; `pip3 install -r requirements.txt`
# from the project root is now sufficient on its own for local dev too.
fastapi
uvicorn[standard]
celery
redis
sqlmodel
psycopg2-binary
boto3
python-multipart
